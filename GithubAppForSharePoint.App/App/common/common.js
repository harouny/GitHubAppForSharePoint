(function (define) {
    "use strict";

    define(['angular'],
        function (angular) {
            var commonModule = angular.module('common', []);
            
            return commonModule;
        });

}(window.define));