(function (define) {
    "use strict";
    define([], function() {

        return function () {
            var self = this;
            self.accountName = "";
            self.displayName = "";
            self.email = "";
        }

    });
}(window.define));
